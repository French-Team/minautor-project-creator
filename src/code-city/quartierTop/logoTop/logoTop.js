/**
 * Logo Top — No-op (la marque "Mermaid Canvas" est définie en HTML dans
 * code-city.js ; on garde l'import pour ne pas casser l'orchestration).
 */
export async function initializeLogoTop() {
    // Le brand est dans le HTML (code-city.js)
}
