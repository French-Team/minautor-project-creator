/**
 * Quartier Left - Helper minimal (la structure HTML vit dans code-city.js)
 * Conservé pour la compatibilité d'import depuis initializeQuartierLeft.
 */
export async function initializeFonctionsMermaidLeft() {
    // No-op : la structure est injectée par code-city.js, la population des
    // catégories est faite par initializeMenuMermaidActionsLeft.
}
